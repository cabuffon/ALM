 {
 "success":true,
 "payload":{
  "agendamientosPorFormulario":[
#foreach(${item} in ${body}) 
	  {"agendamientoPorFormulario":{
	  "Id":"${item.id}",
	  "EstablecimientoFormularioId":"${item.establecimiento_formulario_id}",
	  "Id":"${item.id}",
	  "RBD":"${item.rbd}",
	  "Nombre":"${item.nombre}",
	  "UserId":"${item.user_id}",
	  "Nombre":"${item.nombre}",
	  "Id":"${item.id}",
	  "Nombre":"${item.nombre}",
	  "Fecha":"${item.fecha}"
#if(( $foreach.hasNext ) )
  }},
#{else}
  }}
#end	
#end
]},
  "return":{  
   "code":"200",
   "message":"OK"
  }
}
