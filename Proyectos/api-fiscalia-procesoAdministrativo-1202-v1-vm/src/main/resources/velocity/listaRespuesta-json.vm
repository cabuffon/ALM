 {
 "success":true,
 "payload":{
  "procesosAdministrativosPorRbd":[
#foreach(${item} in ${body}) 
 {"procesoAdministrativoPorRbd":{
  "NumeroActa":"${item.NumeroActa}",
  "FechaIngreso":"${item.FechaIngreso}",
  "TipoPrograma":"${item.TipoPrograma}",
  "EstadoActa":"${item.EstadoActa}"
#if(( $foreach.hasNext ) )
  }},
#{else}
  }}
#end	
#end
]},
  "return":{  
   "code":"200",
   "message":"OK"
  }
}
