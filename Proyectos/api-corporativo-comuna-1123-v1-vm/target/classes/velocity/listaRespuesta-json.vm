{
 "success":true,
 "payload":{
  "comunas":[
#foreach(${item} in ${body}) 
	  {"comuna":{
	  "IdComuna":"${item.id_comuna}",
	  "IdRegion":"${item.id_region}",
	  "Nombre":"${item.nombre}"
#if(( $foreach.hasNext ) )
  }},
#{else}
  }}
#end	  
#end
]},
  "return":{  
   "code":"200",
   "message":"OK"
  }
}
