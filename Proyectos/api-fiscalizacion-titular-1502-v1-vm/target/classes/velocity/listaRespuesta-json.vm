{
 "success":true,
 "payload":{
  "titularesPorCuentaBancaria":[
#foreach(${item} in ${body}) 
	  {"titularPorCuentaBancaria":{
	  "IdCuentaBancaria":"${item.id_cuenta_bancaria}",
	  "IdTitular":"${item.id_titular}",
	  "RutTitular":"${item.rut_titular}",
	  "DvTitular":"${item.dv_titular}",
	  "TipoPersona":"${item.tipo_persona}",
	  "NombreTitular":"${item.nombre_titular}",
	  "PaternoTitular":"${item.paterno_titular}",
	  "MaternoTitular":"${item.materno_titular}",
	  "DireccionTitular":"${item.direccion_titular}",
	  "FechaInicio":"${item.fecha_inicio}",
	  "FechaTermino":"${item.fecha_termino}",
	  "Vigencia":"${item.vigencia}"
#if(( $foreach.hasNext ) )
  }},
#{else}
  }}
#end	
#end
]},
  "return":{  
   "code":"200",
   "message":"OK"
  }
}