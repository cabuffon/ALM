 {
 "success":true,
 "payload":{
  "cuentasBancariasPorSostenedor":[
#foreach(${item} in ${body}) 
	  {"cuentaBancariaPorSostenedor":{
	  "IdCuentaBancaria":"${item.id_cuenta_bancaria}",
	  "NumeroCuenta":"${item.numero_cuenta}",
	  "FechaApertura":"${item.fecha_apertura}",
	  "FechaCierre":"${item.fecha_cierre}",
	  "Banco":"${item.banco}",
	  "NombreSucursal":"${item.nombre_sucursal}",
	  "Vigencia":"${item.vigencia}",
	  "TipoCuenta":"${item.tipoCuenta}",
	  "UtilizacionFondo":"${item.utilizacionFondo}",
	  "OrigenFondo":"${item.origenFondo}",
	  "ProductoAsociado":"${item.productoAsociado}"
#if(( $foreach.hasNext ) )
  }},
#{else}
  }}
#end	
#end
]},
  "return":{  
   "code":"200",
   "message":"OK"
  }
}
