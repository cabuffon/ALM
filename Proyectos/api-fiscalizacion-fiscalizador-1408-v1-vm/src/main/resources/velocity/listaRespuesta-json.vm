{
 "success":true,
 "payload":{
  "fiscalizadoresPorRegion":[
#foreach(${item} in ${body}) 
	  {"fiscalizadorPorRegion":{
	  "Id":"${item.id}",
	  "Nombre":"${item.nombre}",
	  "NombreUsuario":"${item.nombreUsuario}",
	  "Region":"${item.region}",
	  "Imei":"${item.imei}"
#if(( $foreach.hasNext ) )
  }},
#{else}
  }}
#end	
#end
]},
  "return":{  
   "code":"200",
   "message":"OK"
  }
}